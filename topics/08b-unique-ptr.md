# 2. std::unique_ptr

> 🚧 **Note:** This topic is currently being written by Naveen Thumati.

*(Explain why `unique_ptr` is the default choice for modern C++. Discuss exclusive ownership semantics, why copy constructors are deleted, and how `std::move` transfers ownership. Crucially, prove that `unique_ptr` has **zero overhead** compared to a raw pointer.)*
