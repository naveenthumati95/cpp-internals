# 4. std::weak_ptr

> 🚧 **Note:** This topic is currently being written by Naveen Thumati.

*(Explain the problem of cyclic dependencies (e.g., Parent -> Child, Child -> Parent) that cause memory leaks with `shared_ptr`. Show how `weak_ptr` breaks the cycle by observing an object without increasing its strong reference count.)*
